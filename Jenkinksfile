pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/zehraghali/TimeCalculator.git'
            }
        }

        stage('Build') {
            steps {
                withMaven(maven: 'Maven 3.9.9') {
                    sh 'mvn clean install'
                }
            }
        }

        stage('Test') {
            steps {
                withMaven(maven: 'Maven 3.9.9') {
                    sh 'mvn test'
                }
            }
        }

        stage('Code Coverage') {
            steps {
                withMaven(maven: 'Maven 3.9.9') {
                    sh 'mvn jacoco:report'
                }
            }
        }

        stage('Debug Info') {
            steps {
                script {
                    // List the contents of target/surefire-reports and target
                    sh 'echo "Contents of target/surefire-reports:" && ls -al target/surefire-reports'
                    sh 'echo "Contents of target:" && ls -al target'
                }
            }
        }
    }

    post {
        always {
            // Publish JUnit test results
            junit '**/target/surefire-reports/*.xml'

            // Publish JaCoCo code coverage report
            jacoco execPattern: '**/target/jacoco.exec'
        }
    }
}
